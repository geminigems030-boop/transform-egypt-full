TransforM Egypt — Final Consolidated Polish & Optimization Pass. Run a comprehensive audit and execute fixes across the main site (transform-egypt.com), the Lucky funnel (/lucky), and the admin dashboard (/admin). Walk through every page on both EN and AR versions on mobile viewport. Reference luxury beauty brand standards: Vogue Arabia, Net-a-Porter, Tom Ford, Aesop. DO NOT MODIFY: the hero landing video on EN and AR (working as intended, keep exactly), the gold T monogram loading animation, brand voice or copy except where specified, the serif/sans typography pairing, the existing Lucky funnel hero/quiz/wheel structure (only fix what’s listed). PART A — MAIN SITE POLISH. A1 COLOR SYSTEM: Establish single brand gold tokens and replace every yellow/gold instance: –gold-primary #D4B97A (champagne gold main), –gold-deep #B89968 (hover/active), –gold-light #E8D4A2 (highlights), –gold-gradient linear-gradient(135deg, #B89968 0%, #E8D4A2 50%, #B89968 100%) for premium pill buttons. Audit MOST POPULAR tag, newsletter OK button, Book Online button, احجزي الآن pill — all must visually match. A2 CTA HIERARCHY: Three button styles only — Primary (solid gold gradient pill, one per section max), Secondary (outlined gold), Tertiary (gold text link with arrow). Hide floating BOOK NOW pill from any viewport where a primary CTA is already visible. Reduce home page hero from three stacked CTAs to one primary plus one ghost text-link. A3 WHATSAPP BUTTON: Move WhatsApp bubble to bottom-left, restyle with dark background and gold WhatsApp icon. Keep BOOK NOW pill bottom-right. They should never overlap on any viewport. A4 HEADER CLEANUP: Reduce top header to four elements maximum — hamburger, language toggle, shopping bag, T logo. Remove decorative gold-circle-with-dot icon. Reduce header height ~15% on mobile. Fix logo/nav text collision currently visible on Locations page. A5 DECORATIVE ORNAMENTS: Identify all random gold circles/dots floating across the site (especially the one beside the Arabic hero model’s face and scattered between sections). Remove all instances except where they serve as deliberate consistent section dividers. Where kept, standardize: 8px diameter, –gold-primary, 60% opacity, 48px vertical spacing. A6 SERVICE PAGE PADDING: Add padding-top: 80px to content containers on Hair Extensions, Microblading, Lash Extensions, Skin Care, and Nails pages. H1 headlines currently clip at top of viewport. A7 SECTION RHYTHM: Restructure home page to alternate dark/cream backgrounds — black hero, cream Why Choose TransforM, black Real Transformations, cream Signature Services, black founder/Mervat, cream branches, black footer. Cream is #F4ECD8. Currently too many consecutive black-on-black sections. A8 FOOTER CLEANUP: Restructure for consistent alignment. Newsletter OK button must match primary button style. All section headers equal vertical spacing. A9 TAP-TO-CALL: Wrap 01009780008 in a tel link href tel +201009780008 and 01004545700 in a tel link href tel +201004545700 everywhere they appear. A10 RTL/ARABIC AUDIT: Pay particular attention to Arabic version. Fix CTA stacking on Arabic hero (currently overcrowded — five competing CTAs in one viewport). Remove the duplicate italic أنتِ جديدة، اليوم! tagline appearing twice on consultation page (keep top, remove bottom). Fix any text breaking awkwardly in RTL flow. Ensure arrows/chevrons flip in RTL. A11 MOST POPULAR TAG: Currently saturated yellow rectangle on Hair Extensions service card. Restyle as cream background #F4ECD8, –gold-deep 1px border, dark serif text. Editorial callout, not retail tag. A12 PRESS LOGOS GRID: The Trusted by celebrities grid has Heya w Bas — Radwa El Sherbiny wrapping to two lines while other tiles sit on one. Apply white-space nowrap and font-size 0.9em to that tile, or shorten to Heya w Bas. A13 TESTIMONIAL QUOTE MARK: Floating quote mark above testimonials currently oversized and centered. Reduce to 40px, position upper-left of quote container, set –gold-primary at 40% opacity. A14 ITALIC ACCENT: A new you, Today! italic script is currently smaller than headline below. Increase by 30% and treat as brand promise not subtitle. A15 BOUTIQUE NAV: Boutique category list (Tape-In, Keratin, Clip-In, Care, Accessories) currently flat text. Add 40x40px circular product thumbnail next to each category name. A16 DUPLICATE BOOK NOW PILL: Investigate and fix the BOOK NOW pill rendering twice on the Locations page (visible in recent screenshots). PART B — MOTION & DYNAMICS. B1 SCROLL-TRIGGERED REVEALS: Without modifying existing hero video or loading animation, add reveals using Framer Motion. Define reusable variant fadeUp (opacity 0 to 1, translateY 24px to 0, 600ms duration, easing 0.22, 1, 0.36, 1, trigger at 20% in viewport). Apply to all section headlines, body copy, service cards, press logos, testimonial cards. Stagger children by 100ms when multiple elements in same group. B2 BRANCH LIST HOVER STATES: On Six Addresses list rows, on hover/tap: background lightens to rgba(212, 185, 122, 0.06), arrow icon translates 8px right, thin gold underline (1px, –gold-primary) animates in below branch name. 250ms transition. B3 KEN BURNS ON HAIR DETAIL: The static hair detail shot in Signature Services should have a subtle Ken Burns zoom — scale 1.0 to 1.05 over 8 seconds, ease-in-out, infinite alternate. Implement via CSS keyframes. PART C — LUCKY FUNNEL FIXES. C1 SERVER-SIDE PRIZE VALIDATION: Confirm the grand prize cap of 5/day is enforced server-side regardless of where the wheel visually lands. Each spin’s outcome must be determined server-side based on remaining grand prize inventory, not client-side wheel physics. If grand prizes are exhausted, the wheel must land on smaller prizes (15% off, free brows, free deep cond, etc) — never on a grand prize segment. Document this in code comments. C2 WHEEL DUPLICATE GRAND PRIZE: The wheel currently appears to have GRAND PRIZE on two segments. If intentional (giving statistical edge), keep. If accidental rendering, fix so each wheel segment has a unique prize label. C3 TRY AGAIN RETENTION: On the “Try again tomorrow” closing screen, add a secondary CTA: “Or follow @transformegypt on Instagram for daily bonus chances” linking to https://instagram.com/transformegypt. Captures social follow when prize is missed. C4 REMOVE TEST DATA: Ensure “Mariam Atalla” or any other test name is not hard-coded as default value in the form FULL NAME field for real users. Form should render empty by default. C5 MOBILE BADGE TEST: Verify the “DAILY BEAUTY GIVEAWAY · ONLY 5 WINNERS TODAY” badge does not break to two lines awkwardly on iPhone SE (375px) and similar small viewports. PART D — ADMIN DASHBOARD EXPANSION. D1 EXPAND DATA SOURCES: Add form submissions from every public-facing form on transform-egypt.com to flow into /admin alongside existing Lucky leads. Each submission becomes a record in unified leads/bookings table. Forms to capture: Book Your Experience, Free Consultation, Gift Card purchase, Contact/general inquiry, Boutique product order, Newsletter signup. Each record stores: timestamp, source form, full name, phone, email, branch preference, service requested, message/notes, language EN/AR, status (default New), unique submission ID. Use existing database (Postgres or Supabase, whichever /admin already uses). Do not migrate Lucky-spin data — keep alongside, add new tables. D2 MANUAL ENTRY: Add “Log New Lead” button in /admin opening form for staff to manually enter walk-ins, phone bookings, Instagram DM leads, Facebook leads. Same fields as website forms plus source dropdown (Walk-in, Phone, Instagram DM, Facebook DM, Referral, Other). Logged-in user’s name auto-attached as “logged by”. D3 MULTI-USER AUTH: Replace single ADMIN_TOKEN with user accounts. Build users table: email, hashed password (bcrypt), full name, role, branch_assignment (nullable), created_at, last_login. Roles: Owner (sees everything across branches, manages users, sees revenue), Manager (sees their branch, sees their branch revenue), Receptionist (sees their branch, no revenue), Social (sees leads pipeline + website inquiries from all branches, can manually log Instagram/Facebook leads, no branch ops). Login at /admin/login with email + password. Session-based auth, secure HTTP-only cookies, 12-hour expiry. Add /admin/users page (Owner-only) to create/edit/disable users. Keep ADMIN_TOKEN as backup superuser bypass during migration but log warning when used. D4 DASHBOARD HOME: Replace Lucky-spin-only view with unified dashboard. Top: stat cards showing today’s bookings, this week’s bookings, new leads pending follow-up, total clients in database. Below: tabbed view — Bookings, Leads, Clients, Reports. Bookings tab: filterable table by branch/date/status/source, columns name/phone/service/branch/status/date/source. Click row to expand details. Status workflow: New → Contacted → Booked → Completed → No-show → Cancelled. Leads tab: same filters, focused on prospects who haven’t booked. Clients tab: deduplicated by phone, shows name/total bookings/last visit/lifetime spend (placeholder if revenue not tracked)/preferred branch. Reports tab: charts using Recharts — bookings per branch (bar), bookings per source (pie), bookings over time (line), top services. D5 BRANCH FILTERING: Manager and Receptionist queries auto-filter to their assigned branch. Owner and Social see all branches with optional filter dropdown. Hard enforce on backend, never trust frontend. D6 ADMIN UI BRANDING: Match TransforM brand — champagne gold #D4B97A primary, dark theme, serif headlines, sans-serif body. Mobile-responsive (receptionists check on phones at front desk). Add CSV export to every table view. Top search bar searching across name/phone/email/notes. PART E — SEO & PERFORMANCE. E1 SEO HEAD TAGS: Add to head in index.html — Open Graph tags (title, description, image 1200x630, type website), Twitter Card tags, Canonical URL, hreflang tags for EN/AR, JSON-LD HairSalon schema with all 6 branches (name, address, phone, hours, geo coordinates, image), JSON-LD FAQPage schema with 6 common questions. E2 PRERENDERING: Install vite-plugin-prerender. Configure to generate static HTML for all routes at build time so Googlebot, Bing, link scrapers receive pre-rendered content. Real users still get full SPA. Routes to prerender: home, all service detail pages, founder, branches, gift cards, boutique. Do NOT prerender /lucky or /admin (intentionally not indexed). E3 PERFORMANCE: Compress all images to WebP max 1600px wide quality 80. Lazy-load all images below fold (loading lazy). Preload hero video poster image. Audit and remove unused npm dependencies. Run Lighthouse mobile, target Performance 85+, Accessibility 95+, Best Practices 95+, SEO 100. E4 ACCESSIBILITY: All interactive elements need visible focus states (gold outline 2px). All images need descriptive alt text in EN and AR. Color contrast must pass WCAG AA (4.5:1 body, 3:1 headlines). Aria labels on icon-only buttons (hamburger, language toggle, WhatsApp, shopping bag). PART F — FINAL QA. After all above complete, scroll every page top-to-bottom on iPhone Safari simulation in both EN and AR. Document and fix: clipping headlines, CTA overlaps, narrow padding, awkward text breaks, visual inconsistency between EN and AR. Test full Lucky flow EN and AR end-to-end. Test admin login with each role to verify permission enforcement. REPORTING: Before deploying produce checklist showing completed items, skipped items with reasons, and any new issues discovered during audit. Deploy preview build before merging to production. CONSTRAINTS: Do not change brand voice, copy, or messaging except duplicate consultation tagline. Do not modify hero video or T-logo loading animation. Do not redesign typography. Use Tailwind utility classes where possible. Scope custom CSS to component files. Use bcrypt for password hashing. All admin API endpoints must verify session and role on every request, not just on login. Add rate limiting on login endpoint (5 attempts per 15 minutes per IP). Test on iPhone Safari and Chrome Android before declaring complete. This is a multi-session brief — Phase A and B first (visual polish + motion), then C (Lucky fixes), then D (admin expansion), then E (SEO/performance). Deploy preview after each phase for review before continuing.