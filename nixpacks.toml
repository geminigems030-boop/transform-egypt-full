[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
# Only re-install when these files change = huge cache wins
onlyIncludeFiles = [
  "pnpm-lock.yaml",
  "pnpm-workspace.yaml",
  "package.json",
  "artifacts/api-client-react/package.json",
  "artifacts/api-server/package.json"
]
cmds = ["pnpm install --frozen-lockfile --prefer-offline"]

[phases.build]
cmds = [
  "pnpm --filter api-client-react build",
  "pnpm --filter api-server build"
]

# Cache the entire pnpm store between builds
[cache]
paths = [".pnpm-store"]

[start]
cmd = "pnpm --filter api-client-react start"